<Window x:Class="SortingComparison.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение сортировок с шейкером (многопоточность)" Height="520" Width="720">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="36.877"/>
            <ColumnDefinition Width="51*"/>
            <ColumnDefinition Width="449*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        
        <Label Grid.Column="0" Content="Размер массива:" Margin="3,0,3,0" VerticalAlignment="Center" Grid.ColumnSpan="2"/>
        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding ArraySize, UpdateSourceTrigger=PropertyChanged}" Margin="129,3,180,3" Width="140"/>
        <Button Grid.Row="0" Grid.Column="4" Content="Сгенерировать" Command="{Binding GenerateArrayCommand}" Margin="3,3,3,3"/>

        
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="3,0,3,0" VerticalAlignment="Center">
            <Label Content="Потоков (max):" VerticalAlignment="Center"/>
            <TextBox Text="{Binding MaxThreads, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="6,0"/>
            <CheckBox Content="Использовать общий массив" IsChecked="{Binding UseSharedArray}" Margin="12,0" VerticalAlignment="Center"/>
            <Button Content="Прервать все" Command="{Binding CancelAllCommand}" Margin="12,0"/>
        </StackPanel>

       
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="5" Text="{Binding OriginalArrayString}" Margin="3,3,3,3" TextWrapping="Wrap"/>

        
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="3,3,3,3">
            <Button Content="Пузырьковая" Command="{Binding BubbleSortCommand}" Margin="3" Width="120"/>
            <Button Content="Быстрая" Command="{Binding QuickSortCommand}" Margin="3" Width="120"/>
            <Button Content="Вставками" Command="{Binding InsertionSortCommand}" Margin="3" Width="120"/>
            <Button Content="Шейкер" Command="{Binding ShakerSortCommand}" Margin="3" Width="120"/>
        </StackPanel>

       
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="5" Margin="3,3,3,3">
            <TextBlock Text="{Binding BubbleSortResult}" TextWrapping="Wrap"/>
            <ProgressBar Height="14" Minimum="0" Maximum="100" Value="{Binding BubbleProgress}" Margin="0,4,0,8"/>

            <TextBlock Text="{Binding QuickSortResult}" TextWrapping="Wrap"/>
            <ProgressBar Height="14" Minimum="0" Maximum="100" Value="{Binding QuickProgress}" Margin="0,4,0,8"/>

            <TextBlock Text="{Binding InsertionSortResult}" TextWrapping="Wrap"/>
            <ProgressBar Height="14" Minimum="0" Maximum="100" Value="{Binding InsertionProgress}" Margin="0,4,0,8"/>

            <TextBlock Text="{Binding ShakerSortResult}" TextWrapping="Wrap"/>
            <ProgressBar Height="14" Minimum="0" Maximum="100" Value="{Binding ShakerProgress}" Margin="0,4,0,8"/>

            <TextBlock Text="{Binding TotalComparisons}" Margin="0,10,0,0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
